"""
Unaltered variables with immutable values
"""
PREFIX = "5!"
NO_CLASS = (f"You don't have a class yet, but we can fix that! You can view "
            f"the available classes by doing `{PREFIX}classes`, follow the "
            f"instructions to pick a class!")
